#include <iostream>

int main()
{
    long num;
    long factorial = 1; 
    std::cin >> num;
    if (num >= 0)
    {
        for (long i = 2; i <= num; i++)
        factorial = factorial * i;
    }
    std::cout << "factorial = " << factorial << "\n";
}
